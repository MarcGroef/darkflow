to run: 
-adjust DATASET_FOLDER in annotator.py to your dataset directory
 further assumes VOC layout

-python annotate.py
 use WASD to move the bbox around, arrow to adjust size.
 Space to commit a current bbox, will be prompted for label (is focus on image window)
 Enter for next image
